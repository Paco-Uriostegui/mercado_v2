import 'package:flutter/material.dart';

class AppShellProv extends StatelessWidget {
  const AppShellProv({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}